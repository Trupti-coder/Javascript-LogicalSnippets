function findAllSubSequences(str){
    let result=[];

    function generateSubSequences(current,index){
        if(index===str.length){
            if(current!==""){
                result.push(current);
            }
            return;
        }

    }
}