function pascalTriangle(n){
      // Initialize the triangle as an empty array
      let triangle = [];

      for (let i = 0; i < n; i++) {
        // Create a new row
        let row = [];
      }

}