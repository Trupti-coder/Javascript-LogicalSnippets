function minDeletionsForUniqueSubstrings(str){

    let n = str.length;
    let minDeletions = 0;

    for (let len = 1; len < n; len++){

        for (let i = 0; i <= n - len; i++){

            
        }

    }


}