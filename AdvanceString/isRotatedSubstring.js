function isRotatedSubstring(s1,s2){

    if (s1.length !== s2.length) return false;

    let n = s1.length;
    for (let i = 0; i < n; i++){

        let match = true;
        for (let j = 0; j < n; j++){

            
        }

    }

}